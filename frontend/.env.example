# 通常は未設定のまま使用します。
# ブラウザのホスト名に合わせて、フロントエンドが自動的にポート8000のAPIへ接続します。
#
# Render等へデプロイする場合は、ビルド時に backend の公開URLを設定します（末尾スラッシュなし）。
# 例: VITE_API_BASE_URL=https://xxxx.onrender.com
# 実際のURLや秘密情報はリポジトリへコミットしないでください。
# VITE_API_BASE_URL=
