# 設計判断アーカイブ(DR-001〜DR-020)

`docs/design-decisions.md` から移した過去記録。記録内容は移動時の原文であり、
archiveへの移動は失効を意味しない。現行の索引と新しい判断は `docs/design-decisions.md` を参照する。

---

## DR-020: K2娘用ホームは「見通し3項目」を主役にし、できたこと記録・ポイント連動はK3へ送る(2026-07-18)

- **課題感**: K2の画面コンセプトが2案あった。計画のD-01は「見通し3項目」(いまから何する?/明日何がいる?/
  明日何時間目から?)中心、ChatGPT docx設計書のF-01は「できたこと記録」中心で、画面の主役が異なる
  (DR-019 §9-3で「K2設計時に必ず確認」とした宿題)。また docx の時間帯「いつでも」の扱いも未決だった。
- **選択肢**: (a) 見通し3項目中心 / (b) 折衷(見通し主役+できたこと簡易記録) / (c) できたこと記録中心。
  いつでも枠は (x) K2で導入 / (y) K3へ見送り。
- **決定**(ユーザー確認 2026-07-18): **(a) 見通し3項目中心**。できたこと記録とポイント連動
  (おしごと報酬拡張・DR-019)はK3へ。**いつでも枠は (x) K2で導入**——ただし箱と受け口のみ
  (phase='anytime' の受理+母画面の条件付きタブ。ルーチンのシード投入はK3の活動マスタ統合時)。
  デザインモック(`docs/mockups/kurashi-musume-home.html`・紫ゴスロリ・夏休みモード初期状態)は
  **娘本人の確認込みで承認**。夏休みモードでは3項目目を「明日何時に起きる?」に切替(D-05夏休み版の
  起きる時刻をホームへ昇格)、1項目目の文言は「いまから何する?」(ユーザー指示)。
- **理由**: 娘の負担が最小で、帰宅後2〜3タップで見通しが立つ(せめない設計と整合)。おしごと
  (ハロウィン・ラリー)の「楽しみ」としての役割を、記録機能の重複で薄めない。いつでも枠を箱だけ先行
  させるのはK1の daily_limit/display_rule と同じ手筋で、K3でシードを足すだけで画面に現れる。
  なお koekake ドメインへの2点の追記(anytime受理・むすめサマリーAPI)はK2のPRに同梱する——
  別ドメインだが同一デプロイ単位・同一決定範囲であり、PRを4本に割る管理コストの方が高いと判断。
  スペック: `docs/archive/specs/musume-k2/musume-k2-spec.md`。

---

## DR-019: docx設計書(娘の自主活動記録)は「データの箱とラベル」だけK1へ取り込み、記録の分離原則を維持する(2026-07-18)

- **課題感**: ChatGPT(要件定義担当)作成の「くらしリレイ_娘の自主活動記録_Fable用設計書_v0.1.docx」は
  活動マスタのACTキー・3ラベル表現(娘側/ママ側/クイック)・日種別による自動表示切替など有用な提案を
  含む一方、K1計画(`docs/archive/phases/koekake-plan-01.md` §5・基本設計§18)と4点でバッティングしていた:
  ①声かけと完了を1テーブルに統合(record_type方式) ②声かけ文は1活動1文言(回数別文例なし)
  ③娘側記録・ポイント・クイック記録までスコープに含む ④日種別判定エンジンが中核。
- **選択肢**: (a) docx優先でK1計画を作り直す / (b) docxを無視してK1計画のまま進める /
  (c) 整合する部分(データの箱とラベル)だけ取り込み、バッティング部分は既存計画を維持する。
- **決定**(ユーザー確認 2026-07-18): (c)。①prompt_events / completion_events の分離を維持
  ②parent_prompt_label(基本文言)と回数別 prompt_templates の両方を持つ ③K1は母用のみ、
  docxの提案はK2以降へ整理して持ち越し(バッティング箇所はK2設計時に再確認) ④K1は簡易版
  (routine_templates に daily_limit / display_rule の列だけ用意し、生成は is_active+phase のみ)。
  ACTキーは当初「docx対応分のみ・新キーは発行しない」としたが、**同日ユーザー指示で全ルーチン網羅に変更**:
  未対応ルーチン(起床・服薬・出発など)には本アプリ側で ACT-037〜045 を新規発行し、3ラベルも全て埋める
  (発行記録はスペック§3-2の表が正・docx改訂時に同期。運用ルールはdocx§4.1踏襲・ACT-023は欠番)。
  あわせて、**マスタ(ルーチン・声かけ文・時刻)は将来ユーザーが設定画面から編集できるようにする方針**
  (K3。K1はシーダー投入のみだが文言・時刻のハードコード禁止はこの布石)、
  **娘ポイント制は既存おしごと報酬(granted_point_value・reward_collections)の拡張で実装する**
  (別のポイント経済は新設しない。K2)こともユーザー決定。
- **理由**: 分離原則(基本設計§18)と冪等・サーバ値正の実装原則(DR-008/010)は実運用の不具合予防で
  裏付けがあり、docxの表現力(3ラベル・上限・表示条件)は列の追加だけで失わずに済む。K1は新規テーブル
  のみなので箱を今作っておけば、K2でCREATE編集→本番fresh(DR-013)を再度踏まずに拡張できる。
  トレードオフ: routine_templates が当面使わない列を持つ・同一ACTキーが複数行に重複するが、
  K2の活動マスタ統合時に正規化を再検討する。
  スペック: `docs/archive/specs/koekake-k1/koekake-k1-spec.md`(持ち越し整理は同書§9)。

---

## DR-018: タップ記録の取り消しを「キラキラ画面」内に一本化する(2026-07-17)

- **課題感**: カウント方式UI(DR-015)を実機で使ったユーザーの指摘。タップのたびに取り消し導線が
  2つ出ていた —— (a) キラキラ画面(CheerOverlay)内の控えめな「↩ とりけす」(2秒で画面ごと消える)と、
  (b) 画面下部の目立つ黒帯トースト「取り消す」(5秒=キラキラ画面が閉じた後も3秒居残る)。
  ユーザー要望は「取り消しは目立たなく・表示はキラキラ画面と同じ長さ・元画面に戻ったら消えている」。
- **選択肢**: (a) 下部トーストを残しつつ、地味な見た目に変え・表示を2秒に縮め・オーバーレイ閉時に消す /
  (b) 下部トーストを撤去し、取り消しをキラキラ画面内の「↩ とりけす」に一本化する。
- **決定**: (b)。下部トーストを削除。取り消しはキラキラ画面内の既存リンクのみ。
  併せてカウントのピルが連打で残像化する問題(`key={task.count}` による要素の作り直し)も、
  ピルを1個に固定して解消。
- **理由**: ユーザーの3条件(目立たない・キラキラ画面と同じ2秒・戻ったら非表示)を、キラキラ画面内の
  取り消しがそのまま全て満たす。トーストはキラキラ画面(全画面演出)の上に重なる冗長な導線で、
  居残りが「戻ったら消えている」に反していた。トレードオフ: キラキラ画面が閉じた後は取り消し不能に
  なるが、これは「戻ったら取り消しマークを出さない」というユーザーの明示的要望どおり。低リスクな
  「せめない」カウンターであり、誤タップ補正が別途必要になれば後日別導線で検討する。
  スペック: `docs/wip/task-count/task-count-ui-followup.md`。

---

## DR-017: API分離構成(SPA+REST)を維持し、Inertiaへは戻さない(2026-07-17)

- **課題感**: 前作(Inertiaモノリス+Cursor単独実装)に比べ、テスト・検証に大幅に時間がかかっている。
  原因が (1) フロント/バックをREST APIで分離した構成のせいか、(2) スペック→実装(Cursor)→
  レビュー(Codex)→修正指示→再レビューという多段プロセスのせいかを切り分けたかった。
  機能はまだ欲しいものの1/5程度で今後も開発が続くため、「作り直すなら小さい今のうち」という
  タイミング論も含めて、Inertiaへ戻すべきかを判断する必要があった。
- **選択肢**: (a) 今Inertiaへ移行する(モノリス化・セッション認証・1デプロイ化) /
  (b) API分離を維持し、検証コストはテスト自動化とプロセス改善で削る /
  (c) 判断を保留し、再評価の条件(ゲート)だけ決めておく
- **決定**: (b)+(c)。API分離を維持する。検証はプロセス側で軽量化する:
  ① API新機能はPestのfeatureテストを実装の完了条件に含め、docsベースのcurlスモークは
  デプロイ時確認(コールドスタート・本番設定)に限定する
  ② フロントはvitest+MSWで契約を検証し、実機確認はリリース前の1回に絞る
  ③ 小差分(目安200行以下)はレビュー1周で切り上げる
  ④ API+Webを跨ぐ機能でも実装指示書は1枚に統合する(ブランチ/PRは2本のまま)。
  **再評価ゲート**: 実運用開始後の次の大区切り(K3レポート完了、または本格認証が必要になった時点)で、
  残機能がフォーム/帳票CRUD中心なら Inertia 移行を再検討する。
- **理由**:
  - **遅さの主因は構成ではなくプロセスだった**。task-count 1機能でレビュー往復3周・関連docs 8枚・PR 2本。
    多段委譲はトークン費用を下げるが、リードタイムは往復回数に比例して伸びる。ここを直さない限り
    Inertiaに戻しても速くならない。
  - **このアプリの核はクライアント主導の同期設計**。「連打・電波不安定下でのタップ記録」(DR-015/016)が
    本質で、楽観的更新+冪等キー+補償キャンセル+localStorage退避という設計は本番検証済みの最難関部分。
    サーバ主導のInertiaモデルではここを作り直し・再検証することになる。Reactコンポーネント自体は
    Inertiaでも流用できるため「全部書き直し」ではないが、生き残るのは簡単な部分で、死ぬのは一番
    苦労した部分という逆転が起きる。
  - **完成済みの1/5はまさにこの同期基盤**であり、残り4/5はその上に乗る。1機能あたりの限界コストは
    今後下がる(task-countは冪等処理・ごほうび計算をそのまま再利用できた)。「残りが4倍あるから
    構成の税金も4倍」という単純計算にはならない。
  - **サーバはシンガポール**(DR-006)。Inertiaは画面遷移ごとにサーバ往復(約0.1秒+)を踏むが、
    SPAは遷移がローカルで完結し、通信はデータ取得時だけ。スマホでのサクサク感に直結する。
  - **今後の声かけリマインダー等の通知系**は、ブラウザの外からサーバが独立して動く必要があり、
    APIサーバがある構成の方が素直に拡張できる。
  - **移行は数週間の機能フリーズを伴い**、「実運用に足りる機能追加を最優先」(DR-016)と正面から矛盾する。
  - 補足: 現状の接続は「APIキー方式」ではなく**認証なし**(DR-007の意図的な判断)。Inertia化は
    セッション認証(ログイン画面)の新規実装を強制するため、スコープ削減ではなく追加になる。
    認証が必要になったら、まず保存済みの `X-Family-Token` 方式(DR-007)を検討する。

---

## DR-016: こまかい不具合修正より「記録して、見る」運用開始を優先する(2026-07-17)

- **課題感**: 本番環境は動いているが、家庭での実運用が始まっていない。ユーザー(母)の目的は
  「記録できて、記録を見る」という日々の運用を回し始めることであり、並行操作系の既知不具合
  (H1〜3、DR-010)や声かけリマインダー(K1)より先にそこへ到達したい。
- **選択肢**: (a) 計画どおり 8aスモーク → K1 バックエンドへ進む / (b) 記録の実態化
  (トグル→カウント、DR-015)と閲覧(記録を見る最小画面)を先に入れ、運用を開始してから K1 へ進む。
- **決定**: (b)。優先順位 = task-count(BE→FE)→ records-view(閲覧専用の最小画面)→ K1。
  8aスモークはカウント方式を反映した改訂版で、task-count 本番反映後に実施する。
- **理由**: 使われていないアプリを直しても検証にならない。実運用が始まれば「実家庭の連打・電波不安定」
  という本物のテストが回り、H1〜3 の優先度も実測で判断できる。記録閲覧は K3(レポート)の一部前倒しに
  相当し、声かけ中心化計画(DR-012)と矛盾しない。スペック: `docs/archive/specs/task-count/`
  `docs/archive/specs/records-view/`(実装完了につきarchiveへ移動済み)。
- **追記(2026-07-17)**: 本方針どおり task-count(PR #9/#11)と records-view(PR #10)を同日に
  本番反映し、本番DBリセットも実施済み。「記録できて、記録を見る」運用が開始可能になった。

---

## DR-015: 完了記録をトグルからカウント(1タップ=+1)へ変える(2026-07-17)

- **課題感**: 現行のタスク記録は1日1回のトグルで、2回目のタップが取り消しになる。しかし記録したい
  行動(声かけに応える・お手伝い等)は1日に何回もするものが中心で、トグルでは記録が実態と合わず、
  善意のタップで記録が消えてしまう。
- **選択肢**: (a) `task_records` に回数カラムを持たせて increment / (b) 1タップ = 1行のイベントログにする
  (「1日1タスク1件」の部分ユニークインデックスを外す)/ (c) トグルを維持し「×3回やる」等の
  回数別タスク定義を増やす。
- **決定**: (b)。押すたびに `task_records` が1行増え、その日のカウント = 有効レコード件数。
  取り消しは専用の小さな「−」ボタンで最後の1件をキャンセル。
- **理由**: タップごとの時刻が残り、将来の声かけレポート(K3)の素材になる。冪等キーによる二重送信防止と
  ごほうび計算(有効レコード件数ベース)がそのまま使える。(a)は履歴が消え冪等処理が複雑化、(c)は定義の
  無限増殖を招く。副作用としてゲージが「タスク10個」→「タップ10回」で進むが、何回もやる動作をそのまま
  褒める形になるため意図どおりとする(速すぎれば `stamp_size` の config 調整で対応)。
  スペック: `docs/archive/specs/task-count/task-count-spec.md`。
- **追記(2026-07-17 実装完了時)**: UIは実機確認したユーザーの指定により、当初案(チェック丸の回数
  バッジ+専用−ボタン)から**ホームの「クイック記録」カードと同じ操作感**(行タップ=+1・「N件」ピル・
  丸「+」・下部トースト「取り消す」5秒)へ変更した(`task-count-ui-quickcards.md`)。
  送信中(+1のPOST未応答)の取り消しは **operation id 単位の予約 → create確定時に補償cancel**
  という設計で解決(Codexレビュー3周で検証、`codex-review-report-task-count-web*.md`)。
  連打時のPOST並列・応答逆転は既知の並行系バックログ(`docs/archive/phases/phase5-followup-fixes.md`)へ
  積み、根治は member 単位の直列キュー化で行う。BE=PR #9 / FE=PR #11 で本番反映済み。

---

## DR-014: ブランチ運用とドキュメント2層構成を規約化する(2026-07-17)

- **課題感**: K0(バックエンドのマイグレーション統合)が、無関係な frontend 永続化機能のブランチ
  `feature/oshigoto-persistence-frontend` に相乗りし、約3200行の機能と一緒に main へマージされた。
  main は自動デプロイのため「1マージ = 何が本番に出るか」が曖昧だと事故りやすい。
  併せて docs が「依頼→報告→spec」をタスクごとに量産し、`docs/` 直下と `docs/deploy_change/` が
  47ファイルまで膨張。spec も -2/-3/-4 と版が増え、「今の正」が埋もれていた。
- **選択肢**: (a) 現状維持(その都度判断)/ (b) ブランチ命名・分割の規約を持ち、docs をライフサイクルで
  置き場所分けする(恒久 / 進行中 / 完了保管 / 運用)。
- **決定**: (b)。ブランチは「1ブランチ=1デプロイ単位・バックエンド/フロントは別PR・最新mainから切る(相乗り禁止)・
  `<type>/<domain>-<topic>` 命名」。docs は `docs/` 直下(恒久)/ `docs/ops/`(運用)/ `docs/wip/`(進行中)/
  `docs/archive/`(完了保管、`reviews/` `specs/` `phases/` で分類)の4区分。既存の作業用 docs 約20本を
  archive/wip へ移設(本文・相互リンクは変更せず、リンク切れは許容)。規約は AGENTS.md に明記。
- **理由**: main 自動デプロイ環境では「1回のマージで出す範囲」を1つに絞るのが最も事故を防ぐ。docs は
  ライフサイクルで分けることで「今の正」だけが `docs/` 直下に残り、経緯は archive に、結論は DR に集約される。
  就活ポートフォリオとしても「決定の流れ」が追いやすくなる。規約全文: `AGENTS.md`「ブランチ運用」「ドキュメント運用」。

---

## DR-013: 実運用前はDB破棄・再作成で整え、マイグレーションは既存CREATEの編集で一本化する(2026-07-17)

- **課題感**: 声かけリマインダー化(DR-012)で新テーブルが大きく増える。実運用前なのに ALTER マイグレーション
  (例: `add_granted_point_value`)が積み重なると、スキーマの最終形が読み取りにくくなる(ポートフォリオとしても)。
  一方、本番DBは Laravel Cloud に既にあるが、実データはテスト2件のみ。
- **選択肢**: (a) 通常運用どおり ALTER を追加していく / (b) 実運用開始までは既存CREATEマイグレーションを直接編集し、
  DBは `migrate:fresh` で作り直す。実運用開始後は (a) へ移行する
- **決定**: (b)。既存の `granted_point_value` ADD も CREATE へ統合して削除する(K0)。
  本番は 2026-07-17 ユーザー承認の一回限り `migrate:fresh --seed --force` を実行(テスト実データ喪失は承認済み)。
- **理由**: 各テーブルの定義が1ファイルで読み切りになり、環境再現も fresh 一発になる。実データが無い今だけ使える
  自由度で、実運用開始をもってこの方針は終了する。「本番で fresh 絶対禁止」ルール(移行指示書)は
  この承認済みリセットのみ例外とし、以後復活する。計画全文: `docs/archive/phases/koekake-plan-01.md` §4。

---

## DR-012: 声かけリマインダーを中心に据え、既存機能は役割再割当てで共存させる(2026-07-17)

- **課題感**: 新要件(`docs/archive/requirements/` v0.4)は「娘が行動のたびにアプリを見る方式」を明確に否定し、
  母の声かけ支援を中心に据える。これが既存の娘中心機能(おしごと=娘のチェックループ、child-plan)と
  正面からバッティングして見え、どう共存させるか(捨てるのか)が問題だった。
- **選択肢**: (a) 既存の娘向け機能を捨てて新設計に全面置換 / (b) 役割の再割当てで共存 —
  中心=母用声かけリマインダー(新規)、おしごとは新要件が娘に許す2場面(帰宅後・19時)の中の楽しみへ再配置、
  child-plan は見通し3項目へ発展統合 / (c) 新旧をモード切替で併存
- **決定**: (b)。あわせて母ホームの差し替えは即断せず、新画面 `/koekake` を実機でしばらく使ってから判断(K4)。
- **理由**: おしごとの中身は「自分で着替えた」等の過去形の振り返り記録なので、帰宅後・19時にまとめて
  チェックする運用に寄せれば新要件の枠に収まり、ゾンビ図鑑・満月の世界観(娘のモチベーションの核)を
  無傷で残せる。既存の設計原則(監視しない・評価しない・入力ファースト)は新要件と完全に整合しており、
  置換は資産(実装・ポートフォリオ)を失うだけ。(c) は複雑化に見合う利点がない。
  ホームだけは「主従」の問題なので、頭で決めずに実機運用で決めることにした。計画全文: `docs/archive/phases/koekake-plan-01.md`。

---

## DR-011: Phase 8を「安定確認(8a)」と「旧Render停止(8b)」に分け、8bはゲート条件までしない(2026-07-17)

- **課題感**: Phase 7で本番フロントをLaravel Cloud APIへ切替済み。Phase 8は「安定運用の確認→旧Render API停止」だが、
  (1) 旧Render APIは現行の唯一のロールバック先(§8)で、止めると切り戻しが遅くなる。
  (2) 並行系の既知不具合(H1〜3/M1〜3・DR-010)が未修正で、実家庭利用の「子どもの連打・モバイル電波不安定」で
  顕在化しうる。安定確認と停止を一気に進めると、問題が出たときに戻せない。
- **選択肢**: (a) 指示書どおり順にこなし、確認できたら旧Renderを止める / (b) 安定確認(8a)と旧Render停止(8b)を
  分け、8bは「8a合格 + 重大修正の本番反映 or リスク受容 + 実家庭で数日無事」を満たすまで着手しない /
  (c) 旧Renderをすぐ止めてコスト/管理を単純化する
- **決定**: (b)。ランブック `docs/archive/phases/phase8-stability-and-render-stop.md` に 8a チェックリスト
  (curl=Codex委譲 / 実機=ユーザー / 観測=日跨ぎ)と 8b の停止手順(Suspend優先・Deleteはユーザー再確認後)を分離。
  8a は Phase 6 の積み残し(休止復帰後の初回POST・10回達成報酬)も回収する。
- **理由**: 安定確認中は必ず戻せる状態を保ちたい。旧Renderは残置コストが小さく(無料/自動休止)、
  ロールバック保険として置く価値が上回る。停止・削除は不可逆寄りの外向き操作なので、ユーザー明示確認をゲートにした。
  委譲方針(検証はcurlスモークをCodex・実機はユーザー、Fableはブラウザ駆動しない)とも一致。全文はランブック参照。

---

## DR-010: Phase 5をまず本番投入し、並行系の既知不具合は後続で直す(2026-07-17)

- **課題感**: Codexレビュー(`codex-review-report-03.md`)で、フロント永続化に並行・通信断まわりの
  重大3件(応答喪失時の取消で完了復活・並行mutationの順序未保証・起動時GETとキュー再送の競合)+中3件が判明。
  一方、単発・順番どおりの操作は正しく、家族少人数の実利用では顕在化頻度が低い。早くこのフェーズを終えたい。
- **選択肢**: (a) 重大3件を直してから投入 / (b) まず投入し、既知不具合はバックログ化して後続で直す /
  (c) 受容してそのまま(修正しない)
- **決定**: (b)。まず動かし、修正予定は `docs/archive/phases/phase5-followup-fixes.md` に残す。
  根治は「member単位の単一operation processorで直列化+キュー掃き切り後のauthoritative GET」で重大3件を一括。
  修正は安いCursor(composer-2.5 fast:false / grok-4.5)へ委譲し、Codexで再レビューする。
- **理由**: 実害は「子どもの連打」「モバイル再接続」に限られ発生頻度が低く、実利用開始の速さを優先できる。
  ただしPhase 7の本番フロント切替の前後で潰すのが望ましい、と条件付きで先送りにした。バックログと
  テスト追加項目を明記し、忘れないようにした。全文: `codex-review-report-03.md`。

---

## DR-009: 日報の下書きを毎日20時に自動生成する(2026-07-17)

- **課題感**: 日報を書く作業(フォーマット約160行を読む+コミット履歴を読み手向けに翻訳)がFableのトークンを
  多く消費していた。定期的に発生する定型作業なので、繰り返し部分のコストを下げたい。一方で、作業時間・体調・
  感想は本人にしか分からず、フォーマットの禁止事項も「メモにないことを創作しない」と定めている。
- **選択肢**:
  (a) 毎回Fableが対話で全部書く / (b) 20時に完全自動で日報を完成させる /
  (c) 20時に「下書き」まで自動生成し、人にしか分からない項目は【要確認】で残す
- **決定**: (c)。Windowsタスクスケジューラ `KurashiRelay-DailyLogDraft` が毎日20時に
  `.claude/skills/daily-log/scripts/generate-draft.ps1` を実行。決定的な部分(前回日報の特定・
  対象作業日の判定・git材料の収集・メモ書き出し)はスクリプト(無料)、本文の清書はCursor(最安)に委譲し、
  作業時間・体調・感想・次回予定は【要確認】で残す。仕上げ(数値記入・レビュー・コミット)は人+日報スキル。
- **理由**: (b)は禁止事項と衝突し、作業時間・体調が毎回【要確認】の不完全な日報になる。(a)は毎回コストが高い。
  (c)なら繰り返しコストをほぼゼロ(スクリプト+Cursor)に落としつつ、創作しないルールを守れる。
  委譲体制(実装・量産はCursor/Codex、Fableは設計・レビュー)とも一致する。
  ※スケジュール登録はマシンローカル(タスクスケジューラ)でリポジトリには入らない点に注意。

---

## DR-008: フロント永続化はサーバsummaryを唯一の真実にする(2026-07-17)

- **課題感**: 現行フロントはゲージを `INITIAL_JAR(6) + 今日の完了数` で数え、満タンで `-10` して折り返し、
  コイン/ポイントもローカル `useState` で加算、報酬ゾンビ/お菓子も `pickRandom*` でローカル抽選していた。
  一方バックエンドは `gauge_count = lifetime_count % 10`(累積・毎日リセットなし)、報酬は節目POST時に
  サーバが `item_slug` を決めて返す設計。両者のゲージの数え方・報酬の決定主体が食い違い、素朴に繋ぐと
  二重加算やゲージ不整合、報酬の二重演出が起きる。
- **選択肢**: (a) フロントの数え方を残し、APIは保存だけに使う(表示はローカル計算のまま) /
  (b) サーバの `summary`(gauge_count/full_count/coins/points)と POSTレスポンスの `revealed_reward` を
  唯一の真実とし、フロントは楽観的更新→レスポンスで確定同期する / (c) 折衷(ゲージはローカル、報酬はサーバ)
- **決定**: (b)。ゲージ/コイン/ポイントは `summary` を表示し**クライアントで加減算しない**。報酬は
  サーバの `item_slug` をローカルの `ZOMBIES`/`SWEETS` から引いて表示。二重表示は `milestone_number` で防止。
  `localStorage` は本番DBの代替ではなく**オフライン退避**とし、冪等キー固定で再送二重登録を防ぐ。
- **理由**: 履歴から導出する残高方針(DR-003/004)と一貫させ、二重加算・折り返し不整合・報酬二重演出を
  構造的に排除できる。冪等・取消後非復活は本番試験(`phase6-results.md` #3,#4,#8)で実証済みで、
  フロントは冪等キーを固定するだけで安全。初期の見せかけ値(`INITIAL_JAR`/`INITIAL_COINS`/`INITIAL_POINTS`)は
  永続化されておらず、接続後は実DB(0始まり)を正とする。持ち越しは `reward_adjustments` へユーザー確認後に
  別途投入(Phase 7相当)。詳細スペック: `docs/deploy_change/oshigoto-persistence-frontend-spec.md`

---

## DR-007: 更新APIのアクセス保護は今回見送る(2026-07-17)

- **課題感**: Codexレビューで「更新APIが認証なしで公開され、URLを知る第三者が記録を作成・取消できる」
  と重大指摘された。ただし本格認証は今回の移行スコープ外。
- **選択肢**: (a) 家族共有の「あいことば」トークン(端末で初回1回入力→ヘッダ送信) /
  (b) HTTP Basic認証(Laravel Cloud機能) / (c) 今回は保護なし
- **決定**: (c) 保護なし(2026-07-17 ユーザー判断)。(b)は現プランで利用不可と確認済み。
- **理由**: 家族専用の非公開URLで、保存されるのはタスク完了記録のみ(氏名等の個人情報なし)。
  リスクを説明のうえ、利用開始の速さを優先した。将来必要になれば(a)を後付けできる
  (`X-Family-Token` ミドルウェア方式のスペックは `docs/deploy_change/oshigoto-persistence-fix-spec-02.md` §6に保存済み)。

---

## DR-006: APIとDBはシンガポールリージョンに揃える(2026-07-17)

- **課題感**: 指示書は東京リージョン第一候補だったが、Laravel CloudのServerless Postgresは
  作成画面で東京を選べなかった(実値確認)。MySQLなら東京可だが、部分ユニークインデックス非対応で
  実装と不整合、かつ常時稼働で課金増。
- **選択肢**: (a) アプリ=東京+DB=シンガポールの分離 / (b) 両方シンガポール / (c) MySQL東京
- **決定**: (b) アプリ・DBともシンガポール(`https://kurashi-relay-production-olnfy0.laravel.cloud`)
- **理由**: 保存処理は1リクエストでDBと10回以上往復するため、アプリ⇔DB間の距離が体感速度を支配する
  ((a)は1秒級の遅延)。日本⇔シンガポールの1往復(約0.1秒)は許容範囲で、旧Render APIも
  シンガポールなので現状より悪化しない。(c)は実装再作業とレビューやり直しが必要になる。

---

## DR-005: APIとDBだけLaravel Cloudへ移し、ReactはRenderに残す(2026-07-16)

- **課題感**: Render無料APIは15分でスリープし、初回アクセスの待ち時間(30〜60秒)が実利用の障害。
  さらに記録がuseStateのみで、更新すると消える(永続化なし)。明日から家族で実利用したい。
- **選択肢**: (a) Render有料化 / (b) Laravel Cloud(API+Serverless Postgres) / (c) AWS(App Runner+RDS)
- **決定**: (b)。ReactはRender Static Siteに残し、フロント切替は環境変数`VITE_API_BASE_URL`のみで
  行える構成にする。旧Render APIは安定確認まで削除せずロールバック先として残す。
- **理由**: 既契約・カード登録済みで費用を一元確認でき、Laravel最適化された運用画面と
  Scale to Zeroがある。フロントを動かさないことで変更範囲をAPI/DBに限定し、障害時の切り分けと
  環境変数1つでの切り戻しを可能にした。(c)AWSは学習目標としては本命だが、IAM/VPC/RDS等の学習と
  生活アプリの本番移行を同時に行うリスクを避け、別フェーズとした(まず影響の小さいProjNexus等で学ぶ)。

---

## DR-004: 冪等キーごとの報酬応答と移行時単価を固定する(2026-07-17)

- **課題感**: 節目レコードを別キーで重複受理した場合、初回は報酬なしでも再送時にcanonicalレコードの
  報酬が返り、同じキーの応答が変わっていた。また、単価スナップショット列の追加前に存在する記録は
  デフォルト値0のままとなり、移行を境に過去ポイントが失われた。
- **選択肢**: (a) operationごとにレスポンス全体を保存する / (b) レコード作成キーだけ報酬を返し、
  重複受理キーは常に報酬なしとする / (c) 重複受理でもcanonicalレコードの報酬を常に返す。
  既存単価は0のままにする案と、移行時点のtask definition単価で埋める案も比較した。
- **決定**: (b)を採用し、再送時はoperationのキーとレコード作成キーが一致する場合だけ報酬を返す。
  `granted_point_value` 追加時には、既存記録を紐づくtask definitionの現単価で一度だけバックフィルする。
- **理由**: レスポンス保存用の列を増やさず、同一キーの初回と再送を一致させ、重複操作で古いお祝い演出が
  再表示されることを防げる。既存記録の当時単価は保存されていないため完全復元はできないが、現単価での
  バックフィルが移行直前の集計値を維持できる最も安全な近似である。

---

## DR-003: 完了記録から操作履歴とポイント単価を分離する(2026-07-17)

- **課題感**: 同じ完了へ別の冪等キーで到達した操作が保存されず、取消後の再送で完了が復活する経路があった。
  また、現在のタスク単価から過去ポイントを再計算すると、単価変更時に過去の獲得値まで変わってしまう。
- **選択肢**: (a) 完了記録だけで冪等性とポイントを管理し続ける / (b) 受理操作と完了記録を分け、
  完了時単価を記録へ保存する / (c) ポイント残高だけを別カラムで保持する
- **決定**: (b) 全受理キーを `task_record_operations` に保存してcanonicalな完了記録へ結び、
  `task_records.granted_point_value` に完了時単価を保存する。
- **理由**: 操作の再送安全性と業務上の完了状態を分離でき、取消後も受理済み操作を同じ記録へ解決できる。
  ポイントは履歴から導出する方針を維持しながら、当時の単価を再現できる。残高カラム方式より不整合の原因が少ない。
  詳細: `docs/deploy_change/oshigoto-persistence-fix-spec-02.md`

---

## DR-002: アプリ名の表記は「くらしリレー」に決定(2026-07-16)

- **課題感**: アプリ名を「くらしリレー」と「くらしリレイ」のどちらの表記にするか迷いがあった。
- **選択肢**: (a) くらしリレー(標準の長音表記) / (b) くらしリレイ(イ止めでブランド調。
  「レイ」=光/Ray の連想があり、きらきら要素や紫ゴスロリの世界観とは相性が良い)
- **決定**: (a) くらしリレー
- **理由**: アプリのコンセプトが「家事のバトンを家族でつなぐ」ことそのものなので、
  意味が瞬時に伝わる分かりやすさを最優先した。長音「ー」は音・見た目ともにバトンが
  続いていく感じと合い、子どもから祖父母まで一目で読める。検索・ストアでも「リレー」で
  打たれる。(b)は誤記に見える恐れと「霊」連想の小リスクがあり、その洗練・光の気配は
  ロゴやキャッチコピーで補う方針。既存のリポジトリ名・docs表記とも一致し手直し不要。

---

## DR-001: 設計判断を記録する運用を始める(2026-07-16)

- **課題感**: 実装を進めるうちに「なぜこうしたか」が失われていく。就活ポートフォリオとして
  見せるとき・将来アップデートするときに、当初の課題感を思い出す手掛かりがない。
- **選択肢**: (a) コミットメッセージのみに頼る / (b) 1判断1ファイルの厳密なADR / (c) 単一ファイルに軽量に追記
- **決定**: (c) この `design-decisions.md` に軽量フォーマットで追記していく
- **理由**: コミットメッセージは「何をしたか」で「なぜか」が残りにくい。厳密なADRは個人開発には
  重すぎて続かない。1ファイルなら一覧性が高く、ポートフォリオとしてそのまま見せられる。

---

## これ以前の主な判断(バックフィル・要約)

記録開始(2026-07-16)以前の判断は要約のみ。詳細は各ドキュメントを参照。

- **UIリニューアル B案v3 採用** — きぶんカラー5色・入力ファーストの構成。詳細: `docs/ui-redesign-spec.md`
- **子ども(娘)向け画面は紫ベースのゴスロリ調** — さくらピンクではなく、本人の好みに合わせた
  紫+リボン/レース。スキン独立の設計原則とセット。詳細: `docs/design-principles.md`, `docs/archive/design/wireframes/`
- **せめない設計・進捗メタファー等の設計原則** — 家族間で「責めない」体験を軸にする。
  詳細: `docs/design-principles.md`
- **タスク完了時の全画面応援オーバーレイ** — 完了の達成感を最大化する演出。
  詳細: `docs/oshigoto-frontend-spec-4.md`
- **アプリアイコン=おやこしずく(きらきら)** — `doc/icons/final-oyako-shizuku`(feature/app-icons ブランチ)
- **Render 2URL構成(web/api)でのデプロイ** — 無料プランのコールドスタート制約あり
