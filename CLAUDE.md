# CLAUDE.md

Fable(Claude Code)が毎セッション自動で読むファイル。詳細な役割分担・実装ルールは AGENTS.md を参照。

## 恒常ルール(トークン節約)

- **実装はCursorへ委譲**(composer-2.5、小修正も含む)。難所はCodex上位へ。Fableはスペック(docs/*.md)・
  レビュー(`git diff --stat`+要所)・実機検証・デザイン判断のみ。1〜数行のトリビアル修正だけ直接可。
- **セッションが長くなったら、ユーザーにセッション切り替えを提案する**(区切りの直後が好機)。
  切り替え前に、進行状況をメモリ・docs・コミットへ引き継ぐ。
- コミットは確認不要で区切りごとに自律実行。push・PR作成はユーザー確認後。
- 大きなファイルの全文読みやチャットへの長いコード貼り付けをしない。探索は委譲先にさせる。

## 主要資料

- 役割分担・実装ルール・品質確認: `AGENTS.md`
- 設計原則(せめない設計・スキン独立・進捗メタファー等): `docs/design-principles.md`
- くらしのおしごと関連スペック: `docs/oshigoto-frontend-spec*.md` / モック: `docs/mockups/`
